name: research
description: Deep research team — lead planner, cloud analyst, local verifier
members:
  - kind: agent
    id: claude
    role: lead
  - kind: model
    id: gemini-2.0-flash
    provider: gemini
    role: analyst
  - kind: provider
    id: ollama
    role: local-fallback
defaults:
  memory: unified
  # memory: scoped
  # memory_scope:
  #   read: [global, team, workflow]
  #   write: workflow
  #   ttl_hours: 72
  mcp: true
  workflow: review-and-ship
